// middleware.ts
import { withAuth } from "next-auth/middleware";

export default withAuth({
  pages: {
    signIn: "/login",
  },
  secret: process.env.NEXTAUTH_SECRET || "hostelconnect_default_secret_key_12345",
});

export const config = {
  matcher: ["/dashboard/:path*"],
};